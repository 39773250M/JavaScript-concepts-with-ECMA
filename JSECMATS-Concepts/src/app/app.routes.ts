import { Routes } from '@angular/router';
import { ArrayofobjComponent } from './arrayofobj/arrayofobj.component';

export const routes: Routes = [
    {
        path: 'array',
        title: 'Array of object page',
        component: ArrayofobjComponent,
      }

];
